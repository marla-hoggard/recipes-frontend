[build]
  command = "yarn install --production=false && yarn build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

[[edge_functions]]
  path = "/recipe/*"
  function = "spoof-recipe-bot-ua"
